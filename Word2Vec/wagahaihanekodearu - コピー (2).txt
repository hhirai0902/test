マスタA8、スレーブA9の組み合わせにおいて、計測動作が開始されない。
※DTCストアなし(故障未検出)。
マスタA8、スレーブA9の組み合わせにおいて、計測動作が開始されない。
※DTCストアなし(故障未検出)
A9の変更において、InitComplete送信方法を変えた事によって、スレーブA9の初期化完了時、スレーブからInitComplete(Done or Fail)が返らなくなっている。
※A8以前までは、初期化完了(計測可能/不可)確定時、スレーブから自発的にInitComplete(Done or Fail)を返していたが、A9では自発的なInitCompleteは無くし、InitConnectの応答のみでInitCompleteを返す様にした。
また、A9からは、InitConnect定期対応を行っており、A9同士の場合はInitConnect定期送信の応答として、初期化完了(計測可能/不可)を返す作りとなっていた。
⇒マスタがA8以前の場合、InitConnect定期送信がなく、スレーブA9では返らない。
旧ユニットと混在使用される事を考慮していなかった為、変更修正時に検出できなかった。
旧ユニットと混在使用される事を考慮していなかった為、評価の項目として挙げられなかった。